import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

const rootElement = document.getElementById("root");

// Try changing the $background color  variable on Styles.scss file!
ReactDOM.render(<App />, rootElement);
